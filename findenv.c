//// Created by 黄洪飞 on 2023/12/18.//#include "findenv.h"int pCmp(char *name, char *p) {  int i = 0;  while (*name) {    i++;    if (*name++ != *p++) {      return -1;findEnv()    }  }  return i;}char *findEnv(char *name) {  int len = strlen(name);  char **p;  printf("%s\n", name);  for (p = environ; *p; p++) {    if (strncmp(*p, name, len) == 0 && (*p)[len] == '=') {      return *p + len + 1; // Return the value part of the variable    }  }  return NULL;}